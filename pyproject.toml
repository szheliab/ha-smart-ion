[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ha-smart-ion"
version = "0.1.0"
description = "Smart iON Modbus project scaffold"
readme = "README.md"
requires-python = ">=3.12"

[tool.setuptools]
include-package-data = true
